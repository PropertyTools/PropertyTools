<Page x:Class="ControlDemos.CheckBoxListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:p="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="400"
      Title="CheckBoxList Demo">
    <StackPanel Width="300" VerticalAlignment="Center" Margin="20">
        <TextBlock Text="Select permissions:" Margin="0,0,0,4"/>
        <p:CheckBoxList Value="{Binding SelectedPermissions, Mode=TwoWay}" Margin="10"/>
        <TextBlock Text="{Binding SelectedPermissions, StringFormat='Value: {0}'}" Margin="10,4"/>
        <TextBlock Text="Horizontal orientation:" Margin="0,8,0,4"/>
        <p:CheckBoxList Value="{Binding SelectedPermissions, Mode=TwoWay}"
                        Orientation="Horizontal"
                        ItemMargin="0 0 10 0"
                        Margin="10"/>
    </StackPanel>
</Page>
