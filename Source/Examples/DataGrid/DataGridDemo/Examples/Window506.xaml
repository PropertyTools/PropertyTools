<Window x:Class="DataGridDemo.Window506"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
        Title="List&lt;List&lt;Fruit&gt;&gt;" Height="450" Width="400"
        FocusManager.FocusedElement="{Binding ElementName=Grid1}">
    <Grid>
        <p:DataGrid x:Name="Grid1" ItemsSource="{Binding ItemsSource}" RowHeadersSource="{Binding Items302}" ColumnHeadersSource="{Binding Items302}">
        </p:DataGrid>
    </Grid>
</Window>
