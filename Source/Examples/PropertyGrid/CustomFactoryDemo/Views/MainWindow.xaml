<Window x:Class="CustomFactoryDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf" Title="CustomFactoryDemo" Height="350" Width="300">
    <Grid Margin="4">
        <p:PropertyGrid 
            Operator="{Binding CustomPropertyGridOperator}" 
            ControlFactory="{Binding CustomPropertyGridControlFactory}" 
            SelectedObject="{Binding TestObject}"/>
    </Grid>
</Window>
