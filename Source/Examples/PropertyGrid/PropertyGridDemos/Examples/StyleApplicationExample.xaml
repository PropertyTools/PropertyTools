<Window x:Class="PropertyGridDemos.StyleApplicationExample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pt="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
        xmlns:d="clr-namespace:PropertyGridDemos"
        Title="Style Application with IDataErrorInfo (issue #455)" Height="450" Width="800">
    <Window.DataContext>
        <d:StyleApplicationExampleViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Style TargetType="pt:PropertyGrid" x:Key="MyStyle">
            <Setter Property="TabVisibility" Value="VisibleIfMoreThanOne" />
            <Style.Resources>
                <Style TargetType="pt:TextBoxEx">
                    <Setter Property="Background" Value="Yellow" />
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <!-- PropertyGrid bound to Person1 (without IDataErrorInfo) -->
        <pt:PropertyGrid Grid.Column="0"
                        Style="{StaticResource MyStyle}"
                        SelectedObject="{Binding Person1}">
        </pt:PropertyGrid>
        
        <!-- PropertyGrid bound to Person2 (with IDataErrorInfo) -->
        <pt:PropertyGrid Grid.Column="1"
                        Style="{StaticResource MyStyle}"
                        SelectedObject="{Binding Person2}">
        </pt:PropertyGrid>
    </Grid>
</Window>
