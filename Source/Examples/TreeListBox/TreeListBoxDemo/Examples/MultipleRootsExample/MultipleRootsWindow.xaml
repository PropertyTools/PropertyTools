<Window x:Class="TreeListBoxDemo.Examples.MultipleRootsExample.MultipleRootsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TreeListBoxDemo"
        xmlns:pt="http://propertytools.org/wpf"
        Title="Multiple Roots Example - Fix for Issue #282" Height="800" Width="600">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:NodeViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Source="/folder.png" Margin="0 0 4 0"/>
                <pt:EditableTextBlock Grid.Column="1" Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Background="LightYellow" Padding="10" TextWrapping="Wrap">
            <Bold>Multiple Root Items Demo:</Bold> This example demonstrates the TreeListBox with multiple root items.
            Try expanding different root items to see that children correctly appear under their respective parents.
            This showcases the fix for issue #282 where children were incorrectly displayed under the last root item.
        </TextBlock>
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="SelectedItem: "/>
            <TextBlock Text="{Binding SelectedItem.Name, ElementName=tree1}"/>
            <Separator/>
            <TextBlock Text="SelectedItems: "/>
            <TextBlock Text="{Binding SelectedItems.Count, ElementName=tree1}"/>
            <Separator/>
            <TextBlock Text="SelectedIndex: "/>
            <TextBlock Text="{Binding SelectedIndex, ElementName=tree1}"/>
            <Separator/>
            <Button Content="Expand All" Click="ExpandAllClick"/>
            <Button Content="Collapse All" Click="CollapseAllClick"/>
            <Slider Width="100" Value="{Binding Indentation, ElementName=tree1}" Maximum="50"/>
            <TextBlock/>
        </StatusBar>
        <pt:TreeListBox 
            x:Name="tree1" 
            Indentation="12" 
            HierarchySource="{Binding Roots}"
            BorderThickness="0"
            pt:TreeListBoxDragDropHelper.IsDragSource="True" pt:TreeListBoxDragDropHelper.IsDropTarget="True"
            KeyDown="tree1_KeyDown"/>

    </DockPanel>
</Window>
