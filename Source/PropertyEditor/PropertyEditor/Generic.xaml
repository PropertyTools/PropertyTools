<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:PropertyEditorLibrary">
    <Style TargetType="{x:Type local:PropertyEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PropertyEditor}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid Name="PART_Grid">
                            <Grid.Resources>
                                <ResourceDictionary>
                                    <ResourceDictionary.MergedDictionaries>
                                        <ResourceDictionary Source="/PropertyEditor;component/PropertyEditor/Templates/PropertyEditorTemplates.xaml"/>
                                        <ResourceDictionary Source="/PropertyEditor;component/PropertyEditor/Templates/BasicEditors.xaml"/>
                                        <ResourceDictionary Source="/PropertyEditor;component/PropertyEditor/Templates/ExtendedEditors.xaml"/>
                                    </ResourceDictionary.MergedDictionaries>
                                </ResourceDictionary>
                            </Grid.Resources>
                            <TabControl 
                                Name="PART_Tabs" 
                                Focusable="False"
                                ContentTemplate="{StaticResource TabPageTemplate}" 
                                ItemTemplate="{StaticResource TabHeaderTemplate}"/>
                            <ContentControl 
                                Name="PART_Page" 
                                ContentTemplate="{StaticResource TabPageTemplate}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
