namespace PropertyTools
{
    /// <summary>
    ///     Defines the InputDirection of a grid (the moving direction of the current cell when Enter is pressed)
    /// </summary>
    public enum InputDirection
    {
        /// <summary>
        /// Move horizontally.
        /// </summary>
        Horizontal,

        /// <summary>
        /// Move vertically.
        /// </summary>
        Vertical
    }
}